<%= form_for(@tool) do |f| %>
  <% if @tool.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tool.errors.count, "error") %> prohibited this tool from being saved:</h2>

      <ul>
      <% @tool.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, Tool.label(:title) %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description, Tool.label(:description) %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit "Salvar #{Tool.label}", class: 'btn btn-success' %>
  </div>
<% end %>
