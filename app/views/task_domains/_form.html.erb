<%= simple_form_for(@task_domain) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>

  <div class="actions">
    <%= f.button :submit, "Salvar #{TaskDomain.label}", class: 'btn btn-success' %>
  </div>
<% end %>
