<%= form_for(@place_type) do |f| %>
  <% if @place_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@place_type.errors.count, "error") %> prohibited this place_type from being saved:</h2>

      <ul>
      <% @place_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, PlaceType.label(:title) %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description, PlaceType.label(:description) %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :restricted, PlaceType.label(:restricted) %><br>
    <%= f.check_box :restricted %>
  </div>
  <div class="field">
    <%= f.label :common, PlaceType.label(:common) %><br>
    <%= f.check_box :common %>
  </div>
  <div class="actions">
    <%= f.submit "Salvar #{PlaceType.label}", class: 'btn btn-success' %>
  </div>
<% end %>
